{
  "name": "researchband-token-tools",
  "version": "0.1.0",
  "devDependencies": {
    "hardhat": "^2.14.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.7.2",
    "@openzeppelin/contracts": "^4.9.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network sepolia scripts/deploy.js"
  }
}